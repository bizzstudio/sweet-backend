# מצב דמו של iCount

חיבור המערכת לחשבון iCount **נפרד** שאינו הספרים של העסק, כדי להפיק
חשבוניות הדגמה ולראות איך הזרימה עובדת מקצה לקצה.

---

## למה חשבון נפרד ולא "מצב בדיקה"

ל-iCount אין סנדבוקס ואין חשבון דמו ציבורי. כל מסמך שנוצר דרך ה-API נכנס
לספרים של החברה שאליה התחברת. לכן "דמו" כאן פירושו **חברה אחרת ב-iCount**
— חשבון ניסיון או חשבון משני — ולא דגל בבקשה.

לפתיחת חשבון ניסיון: [icount.co.il](https://www.icount.co.il) → הרשמה
לתקופת ניסיון. מתקבלים cid, שם משתמש וסיסמה, והם גם פרטי ה-API (אין טוקן
נפרד).

---

## הפעלה

ב-`.env` של `sweet-backend`:

```
ICOUNT_MODE=demo
ICOUNT_DEMO_CID=<cid של חשבון הניסיון>
ICOUNT_DEMO_USER=<משתמש>
ICOUNT_DEMO_PASS=<סיסמה>
```

הפרטים האמיתיים (`ICOUNT_CID` / `ICOUNT_USER` / `ICOUNT_PASS`) **נשארים
במקומם**. המעבר בין המצבים הוא שינוי של שורה אחת:

| ערך | מה קורה |
|---|---|
| `ICOUNT_MODE=demo` | הכל עובד מול חשבון הדמו; המסד לא משתנה |
| `ICOUNT_MODE=live` או שהמשתנה חסר | התנהגות רגילה, ללא שינוי |

**נדרש ריסטארט לשרת.** המצב נקרא פעם אחת בעלייה, בכוונה — מצב שמשתנה
באמצע ריצה היה יכול להשאיר חצי סגירת חודש בחשבון אחד וחצי בשני.

בלוג העלייה יופיע:

```
[icount] מחובר כ-<user> (חברה: <cid>, מצב: demo) ⚠️ חשבון דמו — לא מופקים מסמכי מס אמיתיים
[billing] ⚠️ ICOUNT_MODE=demo — הסגירה האוטומטית מושבתת; סגירה ידנית מפיקה לחשבון הדמו בלבד
```

אם `ICOUNT_DEMO_CID` זהה ל-`ICOUNT_CID`, ההתחברות נכשלת בכוונה: אין דבר
כזה "דמו על החשבון האמיתי".

הבאנר במסכי החיוב קורא ל-`GET /billing/icount/mode` ולא ל-`/icount/status`:
הראשון קורא משתנה סביבה, השני מתחבר מחדש ל-iCount בכל קריאה. אזהרה על המסך
אינה סיבה לפתוח session חדש בכל טעינה.

---

## מה עובד במצב דמו

**הכול, חוץ מהסגירה האוטומטית.** סגירת חודש ידנית, מסך החשבוניות והגבייה,
רישום תשלום, קבלות, זיכוי, כרטיס הלקוח, חיוב מיידי של לקוח `perDelivery` —
כולם עובדים כרגיל ומציגים את נתוני הדמו.

| פעולה | מצב |
|---|---|
| כל מסכי החיוב והגבייה | ✅ עובדים על נתוני הדמו |
| סגירת חודש אוטומטית (cron של סוף החודש) | ❌ מדלגת |

הסגירה האוטומטית מדלגת מסיבה אחרת לגמרי: היא רצה בלילה בלי אדם, ובדמו
הייתה מחייבת לכיס הדמו בלבד — כלומר החודש האמיתי היה נשאר פתוח בלי שאיש
יידע.

### איפה נשמר מצב הדמו

בכיס נפרד על תעודת המשלוח: `billing.demo.*`, אותם שדות בדיוק כמו
`billing.*` (סטטוס, מספר חשבונית, תאריך חיוב, קבלה, תשלום, זיכויים).

הזרימה האמיתית לא קוראת ולא כותבת לשם לעולם, ואף שאילתה שלה אינה מזכירה
את השדה. הבחירה לאיזה כיס לפנות יושבת במקום אחד — `lib/billing/ledger.js`,
שלושה כלים בסך הכול:

```js
ledger.f("status")      // "billing.status" או "billing.demo.status"
ledger.of(note)         // הכיס לקריאה
ledger.normalize(note)  // מיזוג הכיס אל billing, למי שרק מציג
```

`normalize` הוא הסיבה שאף מסך באדמין לא יודע שיש מצב דמו: הוא ממשיך לקרוא
`note.billing.icountDocNum`, והערך שמגיע אליו הוא של המצב הפעיל.

**תעודה שהדמו מעולם לא נגע בה מוצגת כפי שהיא באמת** ולא מתורגמת ל-"open".
שורה שמראה סטטוס אחד ומספר מסמך של סטטוס אחר גרועה משורה שאומרת את האמת,
ו-`openQuery` ממילא לא מאפשר לחייב בדמו תעודה שחויבה בחשבון האמיתי.

`billingMonth` וסיבת הביטול נשארים משותפים — חודש החיוב הוא עובדה על
התעודה ולא החלטת חיוב, ותעודה מבוטלת מבוטלת בשני המצבים.

### מה זה אומר בפועל

תעודה שחויבה בדמו נשארת `billing.status: "open"` — כלומר היא עדיין ממתינה
לחיוב האמיתי, וסגירת החודש האמיתית תיקח אותה. הדמו לא "צורך" תעודות.

### מייל

**מסמך שנוצר במצב דמו אינו נשלח לאף אחד, בשום מסלול.** ההחלטה יושבת
ב-`baseDoc` שב-`lib/icount/documents.js` — נקודה אחת שכל הפקה עוברת בה —
וגוברת גם על `BILLING_EMAIL_DOCUMENTS=true` וגם על קורא שמעביר
`emailDocument: true` במפורש.

זה נשבר פעם אחת ב-18/08/26: סגירת החודש אינה מעבירה `emailDocument` כלל,
נפלה על מדיניות השליחה, ושלחה חשבונית דמו ללקוח אמיתי. לכן ההגנה עברה
לנקודה המשותפת ולא נשארה אצל הקוראים.

---

## סט מסמכים להתחלה

```bash
node scripts/demo-seed.js        # הלקוח הראשון ברשימה
node scripts/demo-seed.js 723    # לפי מספר לקוח בהנהח"ש
```

בונה ללקוח אחד הצעת מחיר, שתי תעודות משלוח (אחת מתוקים חייבי מע"מ, אחת
פירות פטורים), חשבונית שסוגרת את שתיהן וקבלה עליה — ואז מאמת שהכול הופיע
במסכים ושהרישום האמיתי נשאר נקי. נעצר אם המערכת אינה במצב דמו.

הצעת המחיר ותעודות המשלוח נוצרות במסד כרגיל, כי הן ממילא אינן מסמכי מס
ואינן נשלחות ל-iCount. החשבונית והקבלה נוצרות בחשבון הדמו ונרשמות בכיס
הדמו בלבד.

---

## כרטיסי לקוח

`billing.icountClientId` שבמסד שייך לחשבון האמיתי. במצב דמו הוא **לא
נקרא ולא נכתב** — המיפוי לחשבון הדמו נשמר בזיכרון התהליך בלבד ומת
בריסטארט. המשמעות המעשית: הפעלה ראשונה של כל לקוח יוצרת לו כרטיס בחשבון
הדמו (או מוצאת קיים לפי מספר הלקוח בהנהח"ש), ואין שום זליגה בין החשבונות.

---

## חזרה לחשבון האמיתי

1. `ICOUNT_MODE=live` (או למחוק את השורה) ב-`.env`.
2. ריסטארט לשרת.
3. לוודא בלוג: `מצב: live`.

מרגע זה כל המסכים חוזרים להציג את הרישום האמיתי. אין חובה לנקות — הכיס
של הדמו פשוט מפסיק להיקרא — אבל אפשר:

```bash
node scripts/billing-demo-reset.js          # מה יימחק
node scripts/billing-demo-reset.js --yes    # מחיקה בפועל
```

המסמכים עצמם נשארים בחשבון הדמו ב-iCount. אי אפשר למחוק מסמך שם, וגם אין צורך.

---

## בדיקה

```bash
# המחזור המלא בדמו — חיוב, תשלום, קבלה, זיכוי — ובעיקר: שהרישום האמיתי
# לא נגע (35 בדיקות, iCount מוחלף במדומה)
node scripts/billing-demo-test.js

# מול חשבון הדמו — קורא בלבד, לא מפיק מסמכים
ICOUNT_MODE=demo node scripts/icount-verify.js

# הבדיקות הרגילות. הן מקבעות ICOUNT_MODE=live בעצמן, כדי שיאמתו את
# הזרימה האמיתית גם כשה-.env מחובר לדמו
node scripts/billing-selftest.js
node scripts/billing-integration-test.js
node scripts/billing-email-test.js
```
