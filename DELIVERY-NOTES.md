# תעודות משלוח — אוטומטית מול ידנית

## הבעיה

פירות וירקות נמכרים במשקל. ההזמנה נקלטת עם המשקל ש**הלקוח ביקש**, אבל מה
שנמסר בפועל נקבע על המאזניים ביום האריזה.

עד השינוי הזה תעודת המשלוח נוצרה על **כל** שורות ההזמנה, והחשבונית החודשית
נבנתה ממנה — כלומר הלקוח חויב על משקל שאיש לא שקל.

## הזרימה מעכשיו

```
הזמנה נקלטת  →  סטטוס "טופלה"
   │  (עם המשקל שהלקוח ביקש)
   │
   ├─► תעודה אוטומטית  — רק מוצרים שנמכרים ביחידות
   │        │             נוצרת מיד, בכמות שההזמנה אומרת
   │        │
   │        └─► כל שינוי בהזמנה מרענן אותה, כל עוד לא חויבה
   │
   └─► שורות נשקלות     — יורדות מהתעודה, מוצגות במסך ההזמנה
                          כ"ממתינות לתעודה ידנית"
                             │
                             ▼
                       הקלדת תעודה ידנית
                       הכמות = המשקל שנשקל בפועל
   │
   ▼
סגירת חודש — אוספת את כל התעודות הפתוחות של הלקוח,
             אוטומטיות וידניות כאחת, לחשבונית אחת ב-iCount
```

החשבונית מחייבת את המשקל שנשקל, כי היא נבנית מהתעודות ותעודת המשקל היא
התעודה הידנית.

## מתי נוצרת התעודה, ולמה שם

הטריגר הוא המעבר לסטטוס **"טופלה"** — הסטטוס היחיד שהזמנה נכנסת אליו,
בכל מסלול: קליטה ממייל, קליטה מווצאפ, ואישור ידני של הזמנה שנכנסה
ב"שגיאה בקריאה".

עד 17/08/26 הטריגר היה המעבר ל"נמסרה". הנימוק היה נכון — הזמנה שנקלטה
עדיין משתנה, ותעודה שהופקה בקליטה מתארת משלוח שטרם קרה — אבל המחיר היה
גבוה יותר: "נמסרה" הוא סימון ידני בפאנל, ובלעדיו אין תעודה ולכן גם אין
חשבונית. סחורה שיצאה ואיש לא סימן אותה נשארה בלתי מחויבת.

המענה לנימוק הישן הוא **סנכרון**: `deliveryNotes.syncFromOrder` מרענן את
התעודה בכל שינוי בהזמנה. הוא מחובר ברמת הסכימה של `Order`
([models/Order.js](models/Order.js)) ולא בכל controller בנפרד, כי זו נקודת
הכתיבה היחידה שכל המסלולים עוברים דרכה — כולל מסלול שייכתב מחר.

| מה קרה להזמנה | מה קורה לתעודה |
|---|---|
| כמות תוקנה, פריט ירד, הנחה השתנתה | מתעדכנת, כל עוד היא `open` |
| ההזמנה רוקנה מפריטים | התעודה **מבוטלת** (לא נמחקת — המספר כבר יצא ללקוח) |
| ההזמנה התמלאה שוב | התעודה חוזרת לפעילה, באותו מספר. ביטול שנעשה בידי אדם **אינו** מתבטל כך |
| התעודה כבר חויבה בחשבונית | **אינה משתנה.** הפער נרשם ללוג; תיקון נעשה בחשבונית זיכוי |
| שינוי שאינו נוגע לחיוב (סטטוס, הערה) | לא נוגע בתעודה ואינו מייצר כתיבה |

הסטטוס "נמסרה" כובה בפאנל ואינו מופיע יותר בתפריט, אבל הקוד עדיין מזהה
אותו: הזמנה היסטורית שתסומן כך ידנית תקבל תעודה, כי היא לא תקבל אותה
משום מקום אחר.

### התקנה (חד-פעמי)

```bash
node scripts/setup-single-status.js --dry         # בדיקה, לא משנה כלום
node scripts/setup-single-status.js               # ביצוע
node scripts/setup-single-status.js --backfill    # ובנוסף השלמת תעודות חסרות
```

הסקריפט משנה את השם העברי של `Processing` ל"טופלה" ומכבה את `Delivered`.
השם הפנימי `Processing` **אינו** משתנה — כל הקוד מחפש לפיו, ושינוי שלו היה
מנתק את הקליטה, את הדשבורד ואת אפליקציית הליקוט בבת אחת.

`--backfill` מפיק תעודה להזמנות שכבר ב"טופלה" ואין להן תעודה — הזמנות
שנקלטו לפני השינוי, כלומר סחורה שלא חויבה.

## הפקה באמצע החודש

סגירת החודש הרגילה — לכל הלקוחות — אפשרית רק על חודש שהסתיים, וממילא רצה
אוטומטית בלילה האחרון שלו. **בחירת לקוח מסוים** במסך סגירת החודש פותחת שני
דברים שאינם קיימים בסגירה הגורפת:

1. **הפקה בחודש שעדיין רץ.** לקוח שמבקש חשבונית ב-9 לחודש מקבל אותה.
2. **בחירת תעודות.** התצוגה המקדימה מציגה את התעודות הפתוחות עצמן, ואפשר
   לסמן חלק מהן. מה שלא סומן נשאר פתוח ונאסף בסגירת החודש.

הסגירה הגורפת של חודש רץ נשארה חסומה: לחיצה אחת כזו הייתה מפיקה חשבוניות
חלקיות — ובלתי הפיכות — לכל הלקוחות.

התפיסה האטומית של התעודות (`claimNotes`) מכסה גם את המסלול הזה, ולכן תעודה
שחויבה ידנית באמצע החודש לא תחויב שוב בסגירה האוטומטית שבסופו.

> תעודה שנבחרה ואינה שייכת ללקוח שנבחר פשוט לא תיכנס לחיוב — הסינון על
> המזהים מצטרף לתנאי הלקוח והסטטוס, ואינו מחליף אותם.

## מה מסומן כ"סחורה נשקלת"

הדגל יושב על **הקטגוריה** — `Category.requiresManualNote`. ניתן לעריכה
מהפאנל (עריכת קטגוריה → "סחורה שנשקלת").

הסימון **יורש לתת-הקטגוריות**: מספיק לסמן את "פירות וירקות" פעם אחת, וקטגוריה
שתיווצר מתחתיה מחר תתנהג נכון בלי שאיש יזכור לסמן אותה.

מוצר נחשב נשקל אם הקטגוריה הראשית שלו (`category`) **או** אחת מהקטגוריות
שהוא משויך אליהן (`categories`) מסומנת — כדי ששיוך שיווקי ל"מבצעים" לא
יוציא ירק מהשקילה.

הרזולוציה יושבת ב-[`lib/billing/manualItems.js`](lib/billing/manualItems.js).

> **אם אין אף קטגוריה מסומנת** — הכל מתנהג כמו קודם: תעודה אחת אוטומטית על
> כל ההזמנה. זה גם מצב ברירת המחדל לפני הרצת סקריפט ההתקנה, ולכן פריסת הקוד
> בלי הסקריפט אינה משנה התנהגות.

## התקנה (חד-פעמי, לפני שהקוד החדש רץ בייצור)

```bash
node scripts/setup-manual-delivery-notes.js --dry    # בדיקה, לא משנה כלום
node scripts/setup-manual-delivery-notes.js          # ביצוע
```

הסקריפט עושה שלושה דברים, וכולם חובה:

1. **`kind="auto"` על כל התעודות הקיימות.** האינדקס שמונע שתי תעודות
   אוטומטיות לאותה הזמנה מסונן על `kind="auto"`; מסמך בלי השדה נופל מחוץ
   לסינון ומאבד את ההגנה בשקט.
2. **מחיקת האינדקס הייחודי הישן על `order`.** הוא אוסר שתי תעודות לאותה
   הזמנה — בדיוק מה שההפרדה דורשת. mongoose אינו מוחק אינדקס שהוסר מהסכמה.
3. **סימון קטגוריית "פירות וירקות".** שם אחר: `--category="השם המדויק"`.

הסקריפט אידמפוטנטי — הרצה חוזרת לא משנה דבר.

## הדפסה אוטומטית

כל תעודה שנוצרת — אוטומטית או ידנית — נכנסת מיד לתור הדפסה, וסוכן שרץ על
המחשב שליד המדפסת מוציא אותה תוך כ-10 שניות. אין צורך שמישהו יהיה מול המסך.

**המדריך המלא, כולל התקנה על המחשב במשרד: [`print-agent/README.md`](../print-agent/README.md).**

בקצרה, מה שנוגע לצד השרת:

| רכיב | קובץ |
|---|---|
| התור | `models/PrintJob.js` |
| הוספה לתור | `lib/printing/printJobs.js` — נקרא מ-`deliveryNotes.createFromOrder` ו-`createManual` |
| יצירת ה-PDF | `lib/printing/deliveryNotePdf.js` (HTML → Chromium, A4) |
| ה-API של הסוכן | `controller/printJobController.js`, `routes/printJobRoutes.js` |

הגדרות ב-`.env`:

```ini
PRINT_AGENT_TOKEN=        # חובה. בלעדיו מסלולי ההדפסה מחזירים 503
PRINTING_ENABLED=true     # false = לא נכנסות משימות לתור
DELIVERY_NOTE_COPIES=1    # 1-3. שניים = "מקור" + "העתק", כל אחד בעמוד נפרד
```

**מה קורה כשהתעודה משתנה או מבוטלת:**

| מצב | התנהגות |
|---|---|
| התעודה בוטלה לפני שהנייר יצא | משימת ההדפסה מבוטלת — מסמך מבוטל לא מודפס |
| ההזמנה רוקנה (`syncFromOrder`) | אותו דבר — הביטול מוציא גם מהתור |
| ההזמנה התמלאה שוב והתעודה חזרה לפעילה | נשלחת להדפסה מחדש, אחרת הסחורה יוצאת בלי נייר |
| התעודה השתנתה לפני שהודפסה | הנייר יוצא עם התוכן החדש — ה-PDF נוצר ברגע ההורדה, לא ברגע ההוספה לתור |
| התעודה השתנתה אחרי שהודפסה | הנייר הישן נשאר. "שלח שוב למדפסת" במסך התעודה |

**שני דברים שחשוב לדעת:**

1. ההדפסה מופעלת ברגע שהתעודה **נוצרת**, וזה קורה בקליטת ההזמנה — לפני
   ליקוט ולפני שקילה. `syncFromOrder` ממשיך לעדכן את התעודה אחרי כן,
   והנייר שכבר יצא לא מתעדכן איתה. לתעודה המעודכנת יש "שלח שוב למדפסת"
   במסך התעודה.
2. הוספה לתור לעולם אינה זורקת ואינה נעשית ב-`await`. תקלה בהדפסה לא
   תפיל יצירת תעודה, ובוודאי לא את קליטת ההזמנה שמעליה — הנייר הוא נוחות,
   התעודה היא המסמך.

## היכן זה במסכים

| מסך | מה יש שם |
|---|---|
| הזמנה | התעודה האוטומטית, התעודות הידניות שלה, ורשימת השורות שעדיין ממתינות להקלדה, וכפתור "עריכת פריטים" |
| תעודות משלוח | כפתור "תעודה ידנית" לתעודה עצמאית (משלוח שאין מולו הזמנה), וסינון לפי סוג |
| עריכת קטגוריה | המתג "סחורה שנשקלת" |

## תיקון תעודה אוטומטית — דרך ההזמנה

לתעודה עצמה אין מסך עריכה, ובכוונה: היא נגזרת, וכל שדה שהיה ניתן לעריכה בה
ישירות היה נדרס בסנכרון הבא. התיקון נעשה בהזמנה, בכפתור **"עריכת פריטים"**
שבמסך ההזמנה ([controller/orderController.js](controller/orderController.js) →
`PUT /api/orders/:id/items`, הלוגיקה ב-[lib/orders/editItems.js](lib/orders/editItems.js)).

מה אפשר לשנות שם: כמות, הסרת פריט, הוספת פריט מהקטלוג, דמי משלוח והנחה.
המחיר ליחידה אינו נערך — הוא נקבע במחירון הלקוח, וזו הנקודה שבה מתקנים אותו.

השמירה מפעילה את `syncFromOrder` וממתינה לו, כדי שהמסך יציג את התעודה
המעודכנת ולא את זו שלפני העריכה. שלושה דברים שהמסלול הזה שומר עליהם:

- **תעודה נעולה חוסמת.** חויבה, נתפסה לחיוב, או בוטלה בידי אדם — הבקשה
  חוזרת עם 409 ו-`code: "NOTE_LOCKED"`, והפאנל מציג אישור מפורש. באישור
  ההזמנה מתעדכנת והתעודה **אינה** משתנה; תיקון החיוב נעשה בחשבונית זיכוי.
- **המבצעים מחושבים מחדש** באותו מנוע שרץ בקליטה, כך ש-`offerDiscount` אינו
  נשאר מחושב לכמות ישנה. מוצרי פרס נבנים מחדש; מתנת הצטרפות ומוצר חינם
  מקופון נשמרים כמות שהם.
- **המלאי אינו מתעדכן.** גם מחיקת הזמנה אינה מחזירה מלאי, ותיקון חד-צדדי
  כאן היה מוסיף עוד מקור אמת למספר שכבר אינו מדויק.
- **נעילה אופטימית.** המסך שולח את `updatedAt` שראה, ובקשה שנשענת על מסך
  ישן נדחית (409, `code: "STALE_ORDER"`). העגלה נכתבת כמערך שלם, ובלי זה
  מנהל שני היה מוחק בשקט את התיקון של הראשון.

כל עריכה נרשמת ב-`systemNote` של ההזמנה: מה נוסף, מה השתנה, מה הוסר, ומי ביצע.

## נקודות שחשוב להכיר

**התאריך בטופס הידני קובע את חודש החיוב.** תעודה שהוקלדה באיחור — יש לתארך
אותה ליום המסירה בפועל, אחרת היא תיפול לחודש הבא.

**כמה תעודות ידניות לאותה הזמנה — מותר.** משלוח פירות יוצא לפעמים בפעימות.
שורה שכבר הוקלדה יורדת מרשימת ההמתנה כדי שלא תוקלד פעמיים.

**שתי הגנות שונות מפני חיוב כפול, וכל אחת סוגרת חור אחר:**

| הסיכון | ההגנה |
|---|---|
| אותו טופס נשלח פעמיים (לחיצה כפולה, רענון, ניסיון חוזר אחרי נפילת רשת) | `idempotencyKey` שנוצר בפתיחת הטופס. השליחה השנייה מחזירה את התעודה הראשונה |
| שני אנשים מקלידים את אותן שקילות בנפרד | מק"ט שכבר הוקלד בתעודה ידנית של אותה הזמנה **נחסם**. לתיקון — ביטול התעודה הקיימת; למשלוח נוסף של אותו מוצר — תעודה עצמאית בלי קישור להזמנה |
| שתי תעודות אוטומטיות לאותה הזמנה | אינדקס ייחודי חלקי על `order` עבור `kind="auto"` |

**מה נחסם בהפקה:**

- מק"ט שאין לו מחיר במחירון הלקוח ולא בקטלוג. מחיר 0 **מפורש** מותר — זו
  החלטה מודעת שנרשמת, ולא טעות שנופלת בשקט.
- מק"ט שאינו קיים בקטלוג.
- כמות שאינה מספר חיובי.
- דמי משלוח או הנחה שליליים, והנחה שגדולה מסכום התעודה. זיכוי מוציאים
  במסלול שלו (`creditInvoice`), עם מסמך מס משלו.
- **תאריך עתידי.** תעודה כזו הייתה משויכת לחודש חיוב שטרם הגיע, וסגירת
  החודש — שאוספת `billingMonth <= החודש הנסגר` — לא הייתה נוגעת בה. סחורה
  שנמסרה הייתה יושבת לא מחויבת בלי שאיש ישים לב.

**הנחת ההזמנה מתחלקת יחסית** בין החלק האוטומטי לחלק הנשקל, לפי שיעורם בסכום.
השארית מוחזרת בטופס הידני וממולאת בו מראש — הורדה מלאה מהחלק האוטומטי הייתה
מזכה פעמיים, ואי-החזרה של השארית הייתה מבטלת את ההנחה על הפירות.

> החלוקה מחושבת לפי המשקל ש**הוזמן**, כי בזמן הפקת התעודה האוטומטית המשקל
> שנשקל עדיין לא ידוע. זהו קירוב, והוא היחיד האפשרי במועד הזה.

**דמי המשלוח נשארים על התעודה האוטומטית.** הטופס הידני מקבל 0 כשהיא קיימת,
ואת הסכום המלא כשההזמנה כולה נשקלת ואין תעודה אוטומטית כלל.

## בדיקות

```bash
node scripts/billing-selftest.js
```

הבדיקות רצות מול המסד ומנקות אחריהן, ואינן מפיקות מסמכים ב-iCount. הקבוצות
הרלוונטיות: "הפרדת סחורה נשקלת מהתעודה האוטומטית" ו"תעודת משלוח ידנית —
המשקל שנשקל בפועל".

## מלכודת שכדאי להכיר: `Product.productId`

השדה `Product.productId` **אינו** ה-`_id` של המוצר. הוא מחרוזת בת 24 תווים
שהגיעה מהתבנית שממנה הפרויקט שוכפל, והיא מצביעה על מסמך שאינו קיים — נבדק
על כל 4,320 המוצרים בקטלוג.

כל קוד שמחפש מוצר לפי שורת עגלה חייב להשתמש ב-`line._id`. שימוש
ב-`line.productId` מחזיר "לא נמצא" בלי לזרוק שגיאה, וזה בדיוק סוג הכשל
שמסווג פרי כסחורה יבשה בשקט.
